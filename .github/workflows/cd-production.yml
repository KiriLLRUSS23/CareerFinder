name: CD Production

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    if: contains(github.event.head_commit.message, '[deploy]')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Production deploy ready. Use [deploy] in commit for real deploy."